namespace TemplEnduranceRallyate
{
    using System;
    using System.Collections.Generic;
    using System.Linq;

    public class EnduranceRally
    {
        public static void Main()
        {
            var driversName = Console.ReadLine().Split();
            var zones = Console.ReadLine().Split().Select(double.Parse).ToArray();
            var checkpoint = Console.ReadLine().Split().Select(int.Parse);

            foreach (var driver in driversName)
            {
                double fuel = driver[0];
                for (int i = 0; i < zones.Length; i++)
                {
                    var 
                    if (zones. == )
                    {

                    }
                }
            }
        }
    }
}
